Dependencies:
- pandoc (use something relatively current!)
- python3
- python3-mysql-connector

Migrationsablauf:
- Gruppen + Projekte erstellen, Default-Labels erstellen!
- Benutzer erstellen, temporär als Admin setzen
- Attachments kopieren
- Git-Repository befüllen!
- Importieren :-)
- Admin-Berechtigungen zurücksetzen!

Interessante Tabellen:
Benutzer:
  users (login, mail, first+lastname)
  custom_values ???

Projektliste:
  + projects -> Projekte und Gruppen per Default auf private!

Work packages:
  + versions (Milestones)
  + types (Bug type -> label)
  + categories (Name -> label)
  + statuses (Open / Closed / Rejected)

  + work_package_journals
  + watchers
  + relations
  + hierarchy

Forum:
  + boards
  + messages + message_journals (-> Issues vom Type discussion)

Dateianhänge:
  + attachments (old files are deleted from disk -> not available)
  TODO: wiki / meeting attachments

Wiki:
  + wikis -> project_id to wiki_id
  wiki_redirects ? -> direkt in Wiki-Artikel reinpatchen?
  wiki_content_journals + wiki_pages (Titel zu Name)

Meetings:
  meetings + meeting_contents + meeting_content_journals (-> Wikieinträge)
